// dashboard/public/views/frames.js — «Кадри» screen. Stub: replaced in Task 8.
export function render(container, ctx) {
  container.className = 'screen screen-pad';
  container.innerHTML = '<h2 class="section-title">Кадри</h2>';
}
